#include "main.h"
#include <stdio.h>

/**
 * main - start of program
 * @argc: number of command line integers
 * @argv: number of command line strings
 *
 * Return: Always (0)
 */

int main(int argc __attribute__((unused)), char *argv[] __attribute__((unused)))
{
	printf("%s\n", __FILE__);

	return (0);
}
