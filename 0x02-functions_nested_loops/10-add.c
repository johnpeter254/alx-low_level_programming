#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns result
 * @a: integer to be added to b
 * @b: integer to be added to a
 * Return: sum of a + b
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
